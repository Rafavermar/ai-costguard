#!/usr/bin/env bash
set -euo pipefail

if [ "$#" -lt 1 ]; then
  echo "usage: summarize-log <file>" >&2
  exit 2
fi

tail -300 "$1" | grep -E 'ERROR|FAILED|Exception|Traceback|Caused by|WARN' | head -250
