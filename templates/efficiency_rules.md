# Efficiency rules

- Start with summaries before full files.
- Prefer `short-diff` before full diffs.
- Prefer `safe-grep` over recursive broad searches.
- Use `test-failures-only` for noisy test suites.
- Do not read `.env`, keys, tokens, or state files.
